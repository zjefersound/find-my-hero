import styled from 'styled-components';

export default styled.div`
  display: flex;
  min-height: 100%;
  align-items: center;
  justify-content: center;
  background: radial-gradient(circle at center,var(--secondary), var(--secondary-dark), var(--secondary-dark)); 
  border-top: 0.8rem solid var(--primary);
`;
